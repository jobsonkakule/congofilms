{{ form_start(form) }}

    <div class="row">
        <div class="col-md-4">
            {% if vich_uploader_asset(photo, 'imageFile') is not null %}
                <img src="{{ vich_uploader_asset(photo, 'imageFile') | imagine_filter('thumb') }}" alt="">
            {% endif %}
        </div>
        <div class="col-md-4">{{ form_row(form.caption) }}</div>
        <div class="col-md-4">{{ form_rest(form) }}</div>
    </div>
    <br>
    <button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(form) }}
<div class="mt-5 pt-5"></div>